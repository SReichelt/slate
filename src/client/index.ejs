<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title><%= title %></title>
    <% if (isEmbedded) { %>
      <base href="<%= baseURL %>" />
      <meta http-equiv="Content-Security-Policy" content="default-src <%= cspSource %>; style-src <%= cspSource %> 'unsafe-inline';" />
      <link href="theme-vscode.css" rel="stylesheet" />
    <% } else { %>
      <base href="/" />
      <style>
        body {
          font-family: sans-serif;
        }
      </style>
      <link rel="icon" href="slate.png">
      <link href="theme-default.css" rel="stylesheet" />
    <% } %>
    <link href="js/main.css" rel="stylesheet" />
    <link href="fonts/MathJax/fonts.css" rel="stylesheet" />
    <link href="fonts/LatinModern/fonts.css" rel="stylesheet" />
    <% if (isStatic) { %>
      <link href="static.css" rel="stylesheet" />
      <link href="<%= canonicalURL %>" rel="canonical" />
    <% } else { %>
      <style>
        .loading-screen {
          text-align: center;
        }
        .loading-screen-welcome {
          margin-top: 3em;
          margin-bottom: 3em;
        }
        .loading-screen-title {
          display: block;
          font-size: 300%;
        }
        .loading-screen-loading {
          margin-bottom: 2em;
        }
        .preload-font {
          visibility: hidden;
        }
        .preload-font-regular {
          font-family: 'TeX';
        }
        .preload-font-bold {
          font-family: 'TeX';
          font-weight: bold;
        }
        .preload-font-italic {
          font-family: 'TeX';
          font-style: italic;
        }
        .preload-font-var {
          font-family: 'TeX-Var';
          font-style: italic;
        }
        .preload-font-sans {
          font-family: 'TeX-Sans';
        }
        .preload-font-calligraphic {
          font-family: 'TeX-Calligraphic';
        }
        .preload-font-large {
          font-family: 'TeX-Large';
        }
      </style>
      <meta name="description" content="Slate is a project to build a web-based interactive theorem prover with a focus on abstract mathematics. It is optimized for being easy to learn." />
      <meta name="keywords" content="Interactive Theorem Prover, Proof Assistant, Mathematics, Proofs, Library" />
      <meta name="author" content="Sebastian Reichelt" />
    <% } %>
    <meta name="viewport" content="width=device-width, height=device-height, initial-scale=1" />
  </head>
  <body>
    <div id="app">
      <% if (isStatic) { %>
        <div class="static">
          <%= content %>
        </div>
      <% } else { %>
        <header class="loading-screen">
          <p class="loading-screen-welcome">Welcome to the <span class="loading-screen-title">Slate</span> <% if (isEmbedded) { %>extension for Microsoft Visual Studio Code<% } else { %>web-based interactive theorem prover<% } %>.</p>
          <script src="js/loading.js"></script>
        </header>
        <% if (!isEmbedded) { %>
          <div id="static-nav">
            <p>Slate is a project to build a web-based <a href="https://en.wikipedia.org/wiki/Proof_assistant">interactive theorem prover</a> with a focus on abstract mathematics. It is optimized for being easy to learn.</p>
            <p>Running Slate in your browser requires that JavaScript is enabled.</p>
            <p>Otherwise, please follow these links:</p>
            <ul>
              <li><p><a href="libraries/hlm">HLM Library</a> (statically rendered)</p></li>
              <li><p><a href="https://marketplace.visualstudio.com/items?itemName=sreichelt.slate">Slate Extension</a> for Microsoft Visual Studio Code</p></li>
            </ul>
          </div>
          <script src="js/remove-static-nav.js"></script>
        <% } %>
      <% } %>
    </div>
    <script src="js/main.js" defer="defer"></script>
  </body>
</html>
