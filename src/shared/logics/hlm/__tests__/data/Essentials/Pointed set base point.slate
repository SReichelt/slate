%../../../../../../../data/logics/hlm%

$"Pointed set base point"(i: %Element($"Pointed sets")): %ExplicitOperator {
  definition = [%structuralCases(
    i, $"Pointed sets",
    [{
      constructor = $"Pointed sets"."pointed set",
      parameters = #(
        S: %Set,
        s: %Element(S)
      ),
      value = s
    }]
  )]
}
