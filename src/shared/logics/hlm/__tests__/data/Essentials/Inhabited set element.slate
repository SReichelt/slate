%../../../../../../../data/logics/hlm%

$"Inhabited set element"(i: %Element($"Inhabited sets")): %ExplicitOperator {
  definition = [%structuralCases(
    i,
    $"Inhabited sets",
    [{
      constructor = $"Inhabited sets".instance,
      parameters = #(
        S: %Set,
        s: %Element(S)
      ),
      value = s
    }]
  )]
}
