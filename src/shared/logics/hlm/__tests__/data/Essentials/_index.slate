%../../../../../../../data/logics/library%

$Essentials: %Section {
  logic = 'hlm',
  items = [
    %item($"Natural numbers"),
    %item($sum),
    %item($"less or equal"),
    %item($less),
    %item($Functions),
    %item($value),
    %item($composition),
    %item($bijective),
    %item($Bijections),
    %item($"Cardinal numbers"),
    %item($"Pointed sets"),
    %item($"Pointed set carrier"),
    %item($"Pointed set base point"),
    %item($"pointed natural numbers"),
    %item($"pointed natural numbers with alternative")
  ]
}
