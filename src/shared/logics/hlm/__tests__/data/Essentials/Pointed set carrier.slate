%../../../../../../../data/logics/hlm%

$"Pointed set carrier"(i: %Element($"Pointed sets")): %SetOperator {
  definition = [%setStructuralCases(
    i, $"Pointed sets",
    [{
      constructor = $"Pointed sets"."pointed set",
      parameters = #(
        S: %Set,
        s: %Element(S)
      ),
      value = S
    }]
  )]
}
