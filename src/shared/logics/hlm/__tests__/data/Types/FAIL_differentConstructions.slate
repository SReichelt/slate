%../../../../../../../data/logics/hlm%

/**
 * @expectedError Type mismatch
 *
 * @expectedError Type mismatch
 *
 * @expectedError Type mismatch
 *
 * @expectedError Type mismatch
 */
$FAIL_differentConstructions(
  s: %Element($../Essentials/"Pointed sets"),
  t: %Element($../Essentials/"Natural numbers")
): %Predicate {
  definition = [
    %in(s, $../Essentials/"Natural numbers"),
    %sub($../Essentials/"Pointed sets", $../Essentials/"Natural numbers"),
    %setEquals($../Essentials/"Pointed sets", $../Essentials/"Natural numbers"),
    %equals(s, t)
  ]
}
