%../../../../../../../data/logics/hlm%

/**
 * @expectedError Type mismatch
 */
$FAIL_extractFromPointedNaturalNumbersWithAlternative(
  S: %Set,
  x: %Element(S)
): %StandardTheorem {
  claim = %and(
    %equals(
      $../Essentials/"Pointed set base point"(i = {$../Essentials/"pointed natural numbers with alternative"}),
      $../Essentials/"Natural numbers".zero
    ),
    %equals(
      $../Essentials/"Pointed set base point"(i = {$../Essentials/"pointed natural numbers with alternative"}),
      x
    )
  )
}
