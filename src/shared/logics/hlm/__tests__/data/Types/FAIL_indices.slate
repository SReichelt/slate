%../../../../../../../data/logics/hlm%

/**
 * @expectedError Type mismatch: element of $../Essentials/"Pointed sets" expected
 *
 * @expectedWarning Proof of %in($../Essentials/"Natural numbers".zero, $../Essentials/"Pointed sets") required
 *
 * @expectedError Type mismatch: element of $../Essentials/"Natural numbers" expected
 *
 * @expectedWarning Proof of %in($../Essentials/"Pointed sets"."pointed set"(S = {$../Essentials/"Natural numbers"}, s = {$../Essentials/"Natural numbers".zero}), $../Essentials/"Natural numbers") required
 */
$FAIL_indices(_1: %Binder(
  #(
    i: %Element($../Essentials/"Pointed sets"),
    j: %Element($../Essentials/"Natural numbers")
  ),
  #(a: %Element($../Essentials/"Pointed sets"))
)): %ExplicitOperator {
  definition = [a[i = {$../Essentials/"Natural numbers".zero}, j = {$../Essentials/"Pointed sets"."pointed set"(
      S = {$../Essentials/"Natural numbers"},
      s = {$../Essentials/"Natural numbers".zero}
  )}]]
}
