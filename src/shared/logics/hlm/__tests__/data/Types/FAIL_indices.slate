%../../../../../../../data/logics/hlm%

/**
 * @expectedError Type mismatch
 *
 * @expectedError Type mismatch
 */
$FAIL_indices(i: %Binding(
  $../Essentials/"Pointed sets",
  #(j: %Binding(
    $../Essentials/"Natural numbers",
    #(a: %Element($../Essentials/"Pointed sets"))
  ))
)): %ExplicitOperator {
  definition = [a[
    $../Essentials/"Natural numbers".zero,
    $../Essentials/"Pointed sets"."pointed set"(
        S = {$../Essentials/"Natural numbers"},
        s = {$../Essentials/"Natural numbers".zero}
    )
  ]]
}
