%../../../../../../../data/logics/hlm%

/**
 * @expectedError Indices required
 */
$FAIL_missingIndex(_i: %Binder(
  #(i: %Element($../Essentials/"Natural numbers")),
  #(a: %Element($../Essentials/"Pointed sets"))
)): %ExplicitOperator {
  definition = [a]
}
