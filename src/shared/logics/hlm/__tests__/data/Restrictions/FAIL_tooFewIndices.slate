%../../../../../../../data/logics/hlm%

/**
 * @expectedError Too few indices
 */
$FAIL_tooFewIndices(_i: %Binder(
  #(i: %Element($../Essentials/"Natural numbers")),
  #(_j: %Binder(
    #(j: %Element($../Essentials/"Natural numbers")),
    #(a: %Element($../Essentials/"Pointed sets"))
  ))
)): %ExplicitOperator {
  definition = [a[j = {$../Essentials/"Natural numbers".zero}]]
}
