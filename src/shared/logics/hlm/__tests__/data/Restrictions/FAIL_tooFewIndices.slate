%../../../../../../../data/logics/hlm%

/**
 * @expectedError Too few indices
 */
$FAIL_tooFewIndices(i: %Binding(
  $../Essentials/"Natural numbers",
  #(j: %Binding(
    $../Essentials/"Natural numbers",
    #(a: %Element($../Essentials/"Inhabited sets"))
  ))
)): %ExplicitOperator {
  definition = [a[$../Essentials/"Natural numbers".zero]]
}
