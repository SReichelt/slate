%../../../../../../../data/logics/hlm%

/**
 * @expectedError Superfluous index
 */
$FAIL_tooManyIndices(i: %Binding(
  $../Essentials/"Natural numbers",
  #(a: %Element($../Essentials/"Pointed sets"))
)): %ExplicitOperator {
  definition = [a[
    $../Essentials/"Natural numbers".zero,
    $../Essentials/"Natural numbers".zero
  ]]
}
