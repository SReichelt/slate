%../../../../../../../data/logics/hlm%

/**
 * @expectedError Superfluous index
 */
$FAIL_tooManyIndices(_1: %Binder(
  #(i: %Element($../Essentials/"Natural numbers")),
  #(a: %Element($../Essentials/"Pointed sets"))
)): %ExplicitOperator {
  definition = [a[i = {$../Essentials/"Natural numbers".zero}][]]
}
