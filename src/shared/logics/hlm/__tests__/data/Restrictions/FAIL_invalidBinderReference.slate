%../../../../../../../data/logics/hlm%

/**
 * @expectedError Invalid reference to binder
 */
$FAIL_invalidBinderReference(_i: %Binder(
  #(i: %Element($../Essentials/"Natural numbers")),
  #(a: %Element($../Essentials/"Pointed sets"))
)): %ExplicitOperator {
  definition = [i]
}
