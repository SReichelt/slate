%../../../../../logics/hlm%

$free(𝐌: %Element($../Magmas)): %Predicate {
  display = [$Property(
    operand = 𝐌,
    property = 'free'
  )],
  definition = [%exists(
    #(S: %Subset($../Elements(𝐌 = {𝐌}))),
    formula = $"free on set"(
      𝐌 = {𝐌},
      S = {S}
    )
  )]
}
