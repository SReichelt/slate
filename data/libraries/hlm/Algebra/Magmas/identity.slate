%../../../../logics/hlm%

$identity(𝐌: %Element(
  $Magmas,
  auto = %true
)): %ExplicitOperator {
  display = [$SubSup(
    body = 'id',
    sub = 𝐌
  )],
  definition = [$../../Essentials/Functions/identity(X = {$Elements(𝐌 = {𝐌})})]
}
