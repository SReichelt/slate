%../../../../logics/hlm%

$"Relative complement"(
  S: %Set,
  T: %Subset(S)
): %SetOperator {
  display = [$Operator(
    symbol = '∖',
    operands = [S, T],
    space = ' '
  )],
  definition = [%subset(
    #(x: %Element(S)),
    %not(%in(x, T))
  )]
}
