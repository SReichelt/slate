%../../../../logics/hlm%

$Intersection(
  U: %Set(auto = %true),
  S: %Subset(U),
  T: %Subset(%previous)
): %SetOperator {
  display = [$Operator(
    symbol = '∩',
    operands = [S, T],
    space = ' '
  )],
  definition = [%subset(
    #(x: %Element(U)),
    %and(
      %in(x, S),
      %in(x, T)
    )
  )]
}
