%../../../../logics/hlm%

$"Generalized union"(
  U: %Set(auto = %true),
  I: %Set,
  i: %Binding(
    I,
    #(S: %Subset(U))
  )
): %SetOperator {
  definition = [%subset(
    #(x: %Element(U)),
    %exists(
      #(j: %Element(I)),
      %in(x, S[j])
    )
  )],
  display = [$LargeOperator(
    symbol = '⋃',
    operand = S,
    under = $ElementParameter(
      element = i,
      set = I
    )
  )]
}
