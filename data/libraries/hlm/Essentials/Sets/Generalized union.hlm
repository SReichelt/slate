%../../../../logics/hlm%

$"Generalized union"(
  U: %Set(auto = %true),
  I: %Set,
  i: %Binding(
    I,
    #(S: %Subset(U))
  )
): %SetOperator {
  display = [$LargeOperator(
    symbol = '⋃',
    operand = S,
    under = $ElementParameter(
      element = i,
      set = I
    )
  )],
  definition = [%subset(
    #(x: %Element(U)),
    %exists(
      #(j: %Element(I)),
      formula = %in(x, S[j])
    )
  )]
}
