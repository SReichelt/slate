%../../../../logics/hlm%

$"Union is superset of argument"(
  U: %Set,
  S: %Subset(U),
  T: %Subset(%previous)
): %StandardTheorem {
  claim = %sub(
    S,
    $Union(
      U = {U},
      S = {S},
      T = {T}
    )
  )
}
