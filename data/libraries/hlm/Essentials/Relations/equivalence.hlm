%../../../../logics/hlm%

$equivalence(
  S: %Set(auto = %true),
  ≺: %Element($Relations(S = {S}))
): %Predicate {
  display = [$Property(
    property = 'equivalence relation',
    operand = ≺,
    article = 'an'
  )],
  definition = [%and(
    $preorder(
      S = {S},
      ≺ = {≺}
    ),
    $symmetric(
      S = {S},
      ≺ = {≺}
    )
  )]
}
