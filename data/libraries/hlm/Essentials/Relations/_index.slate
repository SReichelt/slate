%../../../../logics/library%

$Relations: %Section {
  logic = 'hlm',
  items = [
    %item($Relations),
    %item($related),
    %item(
      $predicate,
      title = 'Predicate as relation'
    ),
    %item($converse),
    %item($intersection),
    %item($union),
    %item($mapping),
    %item($"mapping value"),
    %item(
      $Endorelations,
      title = 'Endorelations'
    ),
    %item($restriction),
    %item($equality),
    %item($reflexive),
    %item($connex),
    %item($symmetric),
    %item($antisymmetric),
    %item($transitive),
    %item($preorder),
    %item($"partial order"),
    %item($"total order"),
    %item($"lower bound"),
    %item($"upper bound"),
    %item($"least element"),
    %item($"greatest element"),
    %item($"well-order"),
    %item($equivalence),
    %item(
      $"Equivalence classes",
      title = 'Equivalence classes'
    ),
    %item(
      $"succession",
      title = 'Succession'
    ),
    %item(
      $"lexicographical order",
      title = 'Lexicographical order'
    ),
    %subsection(
      $Closures,
      'Closures'
    )
  ]
}
