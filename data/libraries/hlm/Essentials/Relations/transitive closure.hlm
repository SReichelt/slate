%../../../../logics/hlm%

$"transitive closure"(
  S: %Set(auto = %true),
  ≺: %Element($Relations(S = {S}))
): %ExplicitOperator {
  display = [$Power(
    base = ≺,
    exponent = '+'
  )],
  definition = [$closure(
    S = {S},
    ≺ = {≺},
    P = {%subset(
      #(⊂: %Element($Relations(S = {S}))),
      $transitive(
        S = {S},
        ≺ = {⊂}
      )
    )}
  )]
}
