%../../../../logics/library%

$Properties: %Section {
  logic = 'hlm',
  items = [
    %item($Properties),
    %item($"has property"),
    %item(
      $predicate,
      title = 'Predicate as property'
    )
  ]
}
