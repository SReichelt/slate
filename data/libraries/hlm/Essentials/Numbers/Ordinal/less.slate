%../../../../../logics/hlm%

$less(
  𝐱: %Element($"Ordinal numbers"),
  𝐲: %Element(%previous)
): %Predicate {
  display = [
    $Relation(
      symbol = %neg('<', '≮'),
      operands = [𝐱, 𝐲]
    ),
    $Relation(
      symbol = %neg('>', '≯'),
      operands = [𝐲, 𝐱]
    )
  ],
  definition = [%and(
    $"less or equal"(
      𝐱 = {𝐱},
      𝐲 = {𝐲}
    ),
    %not(%equals(𝐱, 𝐲))
  )]
}
