%../../../../../logics/library%

$Ordinal: %Section {
  logic = 'hlm',
  items = [
    %item($"Ordinal numbers"),
    %item($Elements),
    %item($before),
    %item($Homomorphisms),
    %item($Isomorphisms),
    %item($identity),
    %item($"less or equal"),
    %item($less),
    %item($"Less or equal is a well-order"),
    %item($"Equality criterion"),
    %item($sum),
    %item($product)
  ]
}
