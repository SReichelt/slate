%../../../../../logics/hlm%

$quotient(
  b: %Element($"Rational numbers"),
  a: %Element($"Nonzero numbers")
): %ImplicitOperator {
  display = [$Quotient(
    numerator = b,
    denominator = a
  )],
  parameter = #(x: %Element($"Rational numbers")),
  definition = [%equals(
    b,
    $product(
      a = {x},
      b = {a}
    )
  )]
}
