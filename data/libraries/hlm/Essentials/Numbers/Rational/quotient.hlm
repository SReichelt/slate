%../../../../../logics/hlm%

$quotient(
  b: %Element($"Rational numbers"),
  a: %Element($"Nonzero numbers")
): %ImplicitOperator {
  parameter = #(x: %Element($"Rational numbers")),
  definition = [%equals(
    b,
    $product(
      a = {x},
      b = {a}
    )
  )],
  display = [$Quotient(
    numerator = b,
    denominator = a
  )]
}
