%../../../../../../logics/library%

$Sequences: %Section {
  logic = 'hlm',
  items = [
    %item($converges),
    %item($convergent),
    %item($limit),
    %item($cauchy),
    %item($"sequence sum"),
    %item($"sequence inverse"),
    %item($"sequence difference"),
    %item($"sequence product")
  ]
}
