%../../../../../../logics/hlm%

$limit(
  n: %Binding(
    $../../Natural/"Natural numbers",
    #(a: %Element($../"Rational numbers"))
  ),
  α: %Def($../../../Sequences/Sequences(S = {$../"Rational numbers"}).sequence(n = {
      #(m: %Element($../../Natural/"Natural numbers")),
      {a = {a[m]}}
  })),
  _1: %Constraint($convergent(α = {α}))
): %ExplicitOperator {
  definition = [$"sequence limit"(α = {α})]
}
