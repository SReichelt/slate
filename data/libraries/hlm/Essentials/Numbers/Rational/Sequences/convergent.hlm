%../../../../../../logics/hlm%

$convergent(α: %Element(
  $../../../Sequences/Sequences(S = {$../"Rational numbers"})
)): %Predicate {
  display = [$Property(
    operand = α,
    property = %neg('convergent', 'divergent')
  )],
  definition = [%exists(
    #(l: %Element($../"Rational numbers")),
    formula = $converges(
      α = {α},
      l = {l}
    )
  )]
}
