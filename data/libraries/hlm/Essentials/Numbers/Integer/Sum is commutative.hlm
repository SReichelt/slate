%../../../../../logics/hlm%

$"Sum is commutative": %StandardTheorem {
  claim = $../../Functions/Operations/commutative(
    X = {$Integers},
    Z = {$Integers},
    ∗ = {$../../Functions/Operations/operator(
      X = {$Integers},
      Y = {$Integers},
      Z = {$Integers},
      parameters = #(
        a: %Element($Integers),
        b: %Element(%previous)
      ),
      operator = $sum(
        a = {a},
        b = {b}
      )
    )}
  )
}
