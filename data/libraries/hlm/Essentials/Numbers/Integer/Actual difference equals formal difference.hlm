%../../../../../logics/hlm%

/**
 * @remarks This proposition justifies the notation used for the constructor.
 */
$"Actual difference equals formal difference"(
  n: %Element($../Natural/"Natural numbers"),
  m: %Element(%previous)
): %StandardTheorem {
  claim = %equals(
    $Integers.difference(
        n = {n},
        m = {m}
    ),
    $difference(
      b = {n},
      a = {m}
    )
  )
}
