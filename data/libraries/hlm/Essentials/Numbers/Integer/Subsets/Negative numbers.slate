%../../../../../../logics/hlm%

$"Negative numbers": %SetOperator {
  display = [$SubSup(
    body = 'ℤ',
    sub = '−'
  )],
  definition = [
    $"Segment (less)"(a = {$../../Natural/number(value = 0)}),
    %subset(
      #(a: %Element($../Integers)),
      $../less(
        a = {a},
        b = {$../../Natural/number(value = 0)}
      )
    )
  ]
}
