%../../../../../../logics/hlm%

$"Segment (less)"(a: %Element($../Integers)): %SetOperator {
  display = [$Segment(
    set = 'ℤ',
    symbol = '<',
    limit = a
  )],
  definition = [%subset(
    #(b: %Element($../Integers)),
    $../less(
      a = {b},
      b = {a}
    )
  )]
}
