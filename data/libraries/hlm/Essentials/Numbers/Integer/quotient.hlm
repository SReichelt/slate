%../../../../../logics/hlm%

$quotient(
  b: %Element($Integers),
  a: %Element($Divisors(b = {b}))
): %ImplicitOperator {
  display = [$Fraction(
    numerator = b,
    denominator = a
  )],
  parameter = #(x: %Element($Integers)),
  definition = [%equals(
    b,
    $product(
      a = {x},
      b = {a}
    )
  )]
}
