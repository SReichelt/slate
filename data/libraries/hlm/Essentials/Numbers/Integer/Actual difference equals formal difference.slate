%../../../../../logics/hlm%

/**
 * @remarks This proposition justifies the notation used for the constructor. Note that the subtraction on the right is subtraction on integers, even though _m_ and _n_ are natural numbers.
 */
$"Actual difference equals formal difference"(
  n: %Element($../Natural/"Natural numbers"),
  m: %Element(%previous)
): %StandardTheorem {
  claim = %equals(
    $Integers.difference(
        n = {n},
        m = {m}
    ),
    $difference(
      b = {n},
      a = {m}
    )
  )
}
