%../../../../../logics/hlm%

$Divisors(b: %Element($Integers)): %SetOperator {
  display = [$Function(
    function = $SubSup(
      body = 'Div',
      sub = 'ℤ'
    ),
    arguments = [b]
  )],
  definition = [%subset(
    #(a: %Element($Subsets/"Nonzero numbers")),
    $divisible(
      a = {a},
      b = {b}
    )
  )]
}
