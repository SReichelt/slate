%../../../../../logics/hlm%

$Divisors(b: %Element($Integers)): %SetOperator {
  display = [$Function(
    function = 'Div',
    arguments = [b]
  )],
  definition = [%subset(
    #(a: %Element($"Nonzero numbers")),
    $divisible(
      a = {a},
      b = {b}
    )
  )]
}
