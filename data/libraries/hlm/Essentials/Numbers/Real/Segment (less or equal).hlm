%../../../../../logics/hlm%

$"Segment (less or equal)"(a: %Element($"Real numbers")): %SetOperator {
  definition = [%subset(
    #(b: %Element($"Real numbers")),
    $"less or equal"(
      a = {b},
      b = {a}
    )
  )]
}
