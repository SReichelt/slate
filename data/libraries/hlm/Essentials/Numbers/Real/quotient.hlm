%../../../../../logics/hlm%

$quotient(
  b: %Element($"Real numbers"),
  a: %Element($"Nonzero numbers")
): %ImplicitOperator {
  display = [$Quotient(
    numerator = b,
    denominator = a
  )],
  parameter = #(x: %Element($"Real numbers")),
  definition = [%equals(
    b,
    $product(
      a = {x},
      b = {a}
    )
  )]
}
