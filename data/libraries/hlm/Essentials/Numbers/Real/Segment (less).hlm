%../../../../../logics/hlm%

$"Segment (less)"(a: %Element($"Real numbers")): %SetOperator {
  display = [$Segment(
    set = 'ℝ',
    symbol = '<',
    limit = a
  )],
  definition = [%subset(
    #(b: %Element($"Real numbers")),
    $less(
      a = {b},
      b = {a}
    )
  )]
}
