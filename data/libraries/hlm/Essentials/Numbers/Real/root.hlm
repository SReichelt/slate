%../../../../../logics/hlm%

$root(
  a: %Element($"Nonnegative numbers"),
  n: %Element($../Natural/"Natural numbers")
): %ImplicitOperator {
  display = [$Radical(
    radicand = a,
    degree = n
  )],
  parameter = #(r: %Element($"Nonnegative numbers")),
  definition = [%equals(
    a,
    $power(
      a = {r},
      n = {n}
    )
  )]
}
