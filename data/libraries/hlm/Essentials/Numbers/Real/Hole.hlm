%../../../../../logics/hlm%

$Hole(a: %Element($"Real numbers")): %SetOperator {
  definition = [
    %subset(
      #(b: %Element($"Real numbers")),
      %not(%equals(b, a))
    ),
    $../../Sets/Union(
      U = {$"Real numbers"},
      S = {$"Segment (less)"(a = {a})},
      T = {$"Segment (greater)"(a = {a})}
    )
  ]
}
