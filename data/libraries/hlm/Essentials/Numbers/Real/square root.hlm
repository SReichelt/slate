%../../../../../logics/hlm%

$"square root"(a: %Element($"Nonnegative numbers")): %ExplicitOperator {
  definition = [$root(
    a = {a},
    n = {$../Natural/number(value = 2)}
  )],
  display = [$Radical(radicand = a)]
}
