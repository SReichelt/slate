%../../../../../../logics/library%

$"Generalized sums and products": %Section {
  logic = 'hlm',
  items = [
    %item($"inductive sum (exclusive)"),
    %item($"inductive product (exclusive)")
  ]
}
