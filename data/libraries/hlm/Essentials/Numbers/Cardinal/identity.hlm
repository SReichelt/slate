%../../../../../logics/hlm%

$identity(𝔵: %Element(
  $"Cardinal numbers",
  shortcut = {
    constructor = $"Cardinal numbers".cardinality,
    parameters = #(X: %Set)
  }
)): %ExplicitOperator {
  display = [$SubSup(
    body = 'id',
    sub = 𝔵
  )],
  definition = [$../../Functions/identity(X = {X})]
}
