%../../../../../logics/hlm%

$power(
  𝔵: %Element(
    $"Cardinal numbers",
    shortcut = {
      $"Cardinal numbers".cardinality,
      #(X: %Set)
    }
  ),
  𝔶: %Element(
    %previous,
    shortcut = {
      $"Cardinal numbers".cardinality,
      #(Y: %Set)
    }
  )
): %ExplicitOperator {
  definition = [$"Cardinal numbers".cardinality(S = {$../../Functions/Functions(
      X = {Y},
      Y = {X}
  )})]
}
