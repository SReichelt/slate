%../../../../../logics/hlm%

$identity(𝐱: %Element(
  $"Cardinal numbers",
  shortcut = {
    constructor = $"Cardinal numbers".cardinality,
    parameters = #(X: %Set)
  }
)): %ExplicitOperator {
  display = [$SubSup(
    body = 'id',
    sub = 𝐱
  )],
  definition = [$../../Functions/identity(X = {X})]
}
