%../../../../../logics/hlm%

$sum(
  𝔵: %Element(
    $"Cardinal numbers",
    shortcut = {
      $"Cardinal numbers".cardinality,
      #(X: %Set)
    }
  ),
  𝔶: %Element(
    %previous,
    shortcut = {
      $"Cardinal numbers".cardinality,
      #(Y: %Set)
    }
  )
): %ExplicitOperator {
  definition = [$"Cardinal numbers".cardinality(S = {$../../Sets/"Disjoint union"(
    S = {X},
    T = {Y}
  )})]
}
