%../../../../../logics/hlm%

$product(
  𝔵: %Element(
    $"Cardinal numbers",
    shortcut = {
      $"Cardinal numbers".cardinality,
      #(X: %Set)
    }
  ),
  𝔶: %Element(
    %previous,
    shortcut = {
      $"Cardinal numbers".cardinality,
      #(Y: %Set)
    }
  )
): %ExplicitOperator {
  definition = [$"Cardinal numbers".cardinality(S = {$../../Sets/"Cartesian product"(
      S = {X},
      T = {Y}
  )})]
}
