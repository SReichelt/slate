%../../../../../logics/hlm%

$"Comparison is transitive": %StandardTheorem {
  claim = $../../Relations/transitive(
    S = {$"Natural numbers"},
    ≺ = {$../../Relations/predicate(
      S = {$"Natural numbers"},
      T = {$"Natural numbers"},
      parameters = #(
        m: %Element($"Natural numbers"),
        n: %Element(%previous)
      ),
      predicate = $"less or equal"(
        m = {m},
        n = {n}
      )
    )}
  )
}
