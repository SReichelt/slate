%../../../../../logics/hlm%

$"Comparison is reflexive": %StandardTheorem {
  claim = $../../Relations/reflexive(
    S = {$"Natural numbers"},
    ≺ = {$../../Relations/predicate(
      S = {$"Natural numbers"},
      T = {$"Natural numbers"},
      parameters = #(
        m: %Element($"Natural numbers"),
        n: %Element(%previous)
      ),
      predicate = $"less or equal"(
        m = {m},
        n = {n}
      )
    )}
  )
}
