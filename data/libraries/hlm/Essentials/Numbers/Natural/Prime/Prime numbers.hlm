%../../../../../../logics/hlm%

$"Prime numbers": %SetOperator {
  display = ['ℙ'],
  definition = [%subset(
    #(n: %Element($../"Segment (greater)"(n = {$../number(value = 1)}))),
    $prime(n = {n})
  )]
}
