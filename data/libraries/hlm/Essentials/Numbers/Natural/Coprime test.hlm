%../../../../../logics/hlm%

$"Coprime test"(
  m: %Element($"Segment (greater)"(n = {$number(value = 1)})),
  n: %Element(%previous)
): %StandardTheorem {
  claim = %not($coprime(
    m = {m},
    n = {$product(
      m = {m},
      n = {n}
    )}
  ))
}
