%../../../../../logics/library%

$Operations: %Section {
  logic = 'hlm',
  items = [
    %item($Operations),
    %item($value),
    %item(
      $operator,
      title = 'Operator as operation'
    ),
    %item(
      $"Inner operations",
      title = 'Inner operations'
    ),
    %item($associative),
    %item($commutative)
  ]
}
