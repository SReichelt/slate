%../../../../logics/library%

$"Individual categories": %Section {
  logic = 'hlm',
  items = [
    %item($elements),
    %item($cardinals),
    %item($ordinals),
    %item($magmas),
    %item($categories)
  ]
}
