%../../../../logics/library%

$Categories: %Section {
  logic = 'hlm',
  items = [
    %item($Categories),
    %item($Functors),
    %subsection(
      $"Concrete categories",
      'Concrete categories'
    )
  ]
}
