%../../../../../logics/library%

$Combinators: %Section {
  logic = 'hlm',
  items = [
    %item($identity),
    %item($constant),
    %item($substitution),
    %item($omega),
    %item($"fixed-point")
  ]
}
